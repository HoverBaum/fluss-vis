export const codeMock = `type MyType = string;

/**
 * Much fake, so wow.
 * Long comment
 */
export const fluss = {
  run: (code: string) => {
    return code
  }
};

export AnotherType = {
  name: string;
  age: number;
  id: string;
  property: MyType;
}
`
